export default function App() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">Superadmin Dashboard</p>
        <h1>Manage pandits, bookings, cities, and payouts from one control plane.</h1>
        <p className="copy">
          This starter dashboard is wired for a Vite + React admin surface that can consume the
          backend REST API.
        </p>
      </section>
    </main>
  );
}
