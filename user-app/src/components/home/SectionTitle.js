import React from "react"; import { StyleSheet, Text, TouchableOpacity, View } from "react-native"; import { colors } from "../../theme/homeTheme";
export default function SectionTitle({ title, hindi, onViewAll }) { return <View style={s.row}><View><Text style={s.title}>{title}</Text><Text style={s.hindi}>{hindi}</Text></View>{onViewAll ? <TouchableOpacity onPress={onViewAll}><Text style={s.link}>View all</Text></TouchableOpacity> : null}</View> }
const s = StyleSheet.create({ row: { flexDirection: "row", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 12 }, title: { color: colors.ink, fontSize: 18, fontWeight: "700" }, hindi: { color: colors.muted, fontSize: 10, marginTop: 4 }, link: { color: colors.primary, fontSize: 12, fontWeight: "600", paddingVertical: 3 } });
